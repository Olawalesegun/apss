@model APSS.Web.Dtos.FamilyIndividualDto

<div class="card mt-2">
    <div class="card-body">
        <div class="d-flex "><h5> Confirm Delete for Family of Indiviudal</h5> </div>
        <table class="table user-view-table m-0  ">
            <tbody style="font-size:14px">
                <tr class="">
                    <th>@Html.DisplayNameFor(f=>f.Id)</th>
                    <td> @Model.Id</td>
                </tr>
                <tr>
                    <th>@Html.DisplayNameFor(f=>f.Family.Name)</th>
                    <td> @Model.Family.Name</td>
                </tr>
                <tr>
                    <th>@Html.DisplayNameFor(f=>f.Individual.Name)</th>
                    <td> @Model.Individual.Name</td>
                </tr>
                <tr>
                    <th>@Html.DisplayNameFor(f=>f.IsParent)</th>
                    <td> @Model.IsParent</td>
                </tr>
                <tr>
                    <th>@Html.DisplayNameFor(f=>f.IsProvider)</th>
                    <td> @Model.IsProvider</td>
                </tr>
            </tbody>
        </table>
        <br />
        <div class=" d-inline pt-2">
            <a class="btn btn-danger btn-rounded  " asp-route-Id="@Model.Id" asp-controller="Family" asp-action="DeleteFamilyIndividual">Confirm</a>
            <a class="btn btn-primary btn-rounded mx-3" asp-controller="Family" asp-action="GetFamilies">Cancel</a>
        </div>

    </div>
</div>