@model VoluntaryDto

<div class="card mt-2">
    <div class="card-body">
        <div class="d-flex "><h5>Confirm Delete Voluntary</h5> </div>
        <table class="table user-view-table m-0  ">
            <tbody style="font-size:14px">
                <tr class="">
                    <th class="align-middle">@Html.DisplayNameFor(m => m.Id)</th>
                    <td> @Model.Id</td>
                </tr>
                <tr>
                    <th class="align-middle">@Html.DisplayNameFor(m => m.Name)</th>
                    <td> @Model.Name</td>
                </tr>
                 <tr>
                    <th class="align-middle">@Html.DisplayNameFor(m => m.Field)</th>
                    <td> @Model.Field</td>
                </tr>
                <tr>
                    <th class="align-middle">@Html.DisplayNameFor(m => m.IndividualName)</th>
                    <td> @Model.IndividualName</td>
                </tr>
                 <tr>
                    <th class="align-middle">@Html.DisplayNameFor(m => m.CreatedAt)</th>
                    <td> @Model.CreatedAt</td>
                </tr>
                 <tr>
                    <th class="align-middle">@Html.DisplayNameFor(m => m.ModifiedAt)</th>
                    <td> @Model.ModifiedAt</td>
                </tr>
            </tbody>
        </table>
        <br />
       <div class="mb-4  mt-4">
           <form method="post" asp-route-Id="@Model.Id" asp-area="Population" asp-controller="Voluntaries" asp-action="DeleteVoluntary">
               <button type="submit" class="btn btn-danger btn-rounded" >Confirm</button>
                <a onClick="history.go(-1); return false;" class="btn btn-link   mx-2" style="background-color:#DCEEFB">Cancel</a>
                </form>
            </div>
    </div>
</div>