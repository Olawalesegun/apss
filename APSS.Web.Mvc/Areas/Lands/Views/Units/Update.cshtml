@model LandProductUnitDto
@{
    ViewData["title"] = "Update Unit Page";
}
<div class="container-fluid">
    <div class="col-sm-12 mx-sm-2 col-lg-8">
        <form method="post" action="@Routes.Dashboard.Lands.Units.Update.FullPath" asp-antiforgery="true" class="needs-validation" novalidate>
            <!--  column puts for the Land Product Expense Type -->
            <input type="hidden" asp-for="Id" />
            <div class="center d-flex justify-content-center mb-3 ">
                <h3 class=" ">
                    Update Land Unit
                </h3>
            </div>
            <div class=" mb-3">
                <div class="input-group has-validation">
                    <span class="input-group-text">@@</span>
                    <div class="form-floating col">
                        <input type="text" asp-for="Name" class="form-control" placeholder=" " aria-describedby="nameValidation" required />
                        <label class="form-label" asp-for="Name"></label>
                    </div>
                    <span id="nameValidation" class="d-block invalid-feedback" asp-validation-for="Name"></span>
                </div>
            </div>
            <!-- Submit button -->
            <div class="mt-4">
                <button type="submit" class="btn btn-primary px-5 my-4">Update</button>
            </div>
        </form>
    </div>
</div>